'use client' ; import { FormEvent, useEffect, useState } from 'react' ; type Store = { id: string; name: string; slug: string; description: string | null; logoUrl: string | null; phone: string | null; status: string; updatedAt: string; } ; type StoreResponse = { success: boolean; message?: string; store?: Store; } ; async function readJson(response: Response): Promise<StoreResponse> { const data = (await response.json()) as StoreResponse ; if (!response.ok || !data.success) { throw new Error(data.message || 'حدث خطأ في الطلب') ; } return data ; } export function useStoreSettings() { const [store, setStore] = useState<Store | null>(null) ; const [loading, setLoading] = useState(true) ; const [saving, setSaving] = useState(false) ; const [uploading, setUploading] = useState(false) ; const [name, setName] = useState('') ; const [slug, setSlug] = useState('') ; const [description, setDescription] = useState('') ; const [phone, setPhone] = useState('') ; const [error, setError] = useState('') ; const [message, setMessage] = useState('') ; async function loadStore() { try { setLoading(true) ; setError('') ; const response = await fetch('/api/store', { method: 'GET', cache: 'no-store', credentials: 'include' }) ; const data = await readJson(response) ; if (data.store) { setStore(data.store) ; setName(data.store.name) ; setSlug(data.store.slug) ; setDescription(data.store.description ?? '') ; setPhone(data.store.phone ?? '') ; } } catch (err) { console.error('Load store failed:', err) ; setError(err instanceof Error ? err.message : 'حدث خطأ أثناء تحميل بيانات المتجر') ; } finally { setLoading(false) ; } } useEffect(() => { void loadStore() ; }, []) ; async function handleSubmit(event: FormEvent<HTMLFormElement>) { event.preventDefault() ; if (saving) return ; const trimmedName = name.trim() ; const trimmedSlug = slug.trim() ; if (!trimmedName) { setError('اسم المتجر مطلوب') ; return ; } if (!trimmedSlug) { setError('رابط المتجر مطلوب') ; return ; } setSaving(true) ; setError('') ; setMessage('') ; try { const response = await fetch('/api/store', { method: 'PATCH', headers: { 'Content-Type': 'application/json' }, credentials: 'include', body: JSON.stringify({ name: trimmedName, slug: trimmedSlug, description: description.trim() || null, phone: phone.trim() || null, }), }) ; const data = await readJson(response) ; if (data.store) { setStore(data.store) ; } setMessage('تم تحديث بيانات المتجر بنجاح') ; } catch (err) { console.error('Update store failed:', err) ; setError(err instanceof Error ? err.message : 'حدث خطأ أثناء تحديث بيانات المتجر') ; } finally { setSaving(false) ; } } async function uploadLogo(file: File) { if (uploading) return ; setUploading(true) ; setError('') ; setMessage('') ; try { const formData = new FormData() ; formData.append('file', file) ; const response = await fetch('/api/store/logo/upload', { method: 'POST', credentials: 'include', body: formData, }) ; const data = await response.json() ; if (!response.ok || !data.success) { throw new Error(data.message || 'فشل رفع الشعار') ; } if (data.logoUrl && store) { setStore({ ...store, logoUrl: data.logoUrl }) ; } setMessage('تم رفع شعار المتجر بنجاح') ; } catch (err) { console.error('Upload logo failed:', err) ; setError(err instanceof Error ? err.message : 'حدث خطأ أثناء رفع الشعار') ; } finally { setUploading(false) ; } } return { store, loading, saving, uploading, name, slug, description, phone, error, message, setName, setSlug, setDescription, setPhone, loadStore, handleSubmit, uploadLogo, } ; }