import { Button } from '@/components/ui/Button' ; import { SearchIcon, FilterIcon, ChevronLeftIcon, ChevronRightIcon, XIcon } from '@/components/icons' ; import { useProducts } from './hooks/useProducts' ; import { ProductsStats } from './components/ProductsStats' ; import { ProductsTable } from './components/ProductsTable' ; import { ProductFormModal } from './components/ProductFormModal' ; import { ProductsSkeleton } from './components/ProductsSkeleton' ; export default function StoreProductsPage() { const state = useProducts() ; const { products, loading, deletingId, error, activeProducts, availableProducts, showForm, loadProducts, openCreateForm, openEditForm, deleteProduct, searchQuery, setSearchQuery, filters, setFilter, clearFilters, page, pagination, goToPage, hasActiveFilters, } = state ; return ( <div className='mx-auto max-w-7xl px-6 py-8'> {error && ( <div className='mb-5 rounded-2xl border border-danger/30 bg-danger-bg px-5 py-4 text-sm font-medium text-danger'> {error} </div> )} <div className='mb-6 flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between'> <div> <h1 className='font-display text-2xl font-semibold text-ink'> إدارة المنتجات </h1> <p className='mt-1 text-sm text-ink-soft'> أضف منتجات متجرك وتابع حالتها وتوفرها. </p> </div> <Button onClick={openCreateForm}>إضافة منتج</Button> </div> {/* Search + Filters Bar */} <div className='mb-6 flex flex-wrap items-center gap-3'> <div className='flex flex-1 items-center gap-2 rounded-xl border border-line bg-surface px-3 py-2 min-w-[200px]'> <SearchIcon width={16} height={16} className='text-ink-soft' /> <input type='text' value={searchQuery} onChange={(e) => setSearchQuery(e.target.value)} placeholder='البحث في المنتجات...' className='flex-1 bg-transparent text-sm outline-none placeholder:text-ink-soft/70' /> {searchQuery && ( <button type='button' onClick={() => setSearchQuery('')} className='text-ink-soft hover:text-ink'> <XIcon width={14} height={14} /> </button> )} </div> <div className='flex items-center gap-2 rounded-xl border border-line bg-surface px-3 py-2'> <FilterIcon width={16} height={16} className='text-ink-soft' /> <select value={filters.categoryId} onChange={(e) => setFilter('categoryId', e.target.value)} className='bg-transparent text-sm outline-none'> <option value=''>كل التصنيفات</option> {state.categories.map((cat) => ( <option key={cat.id} value={cat.id}>{cat.name}</option> ))} </select> </div> <div className='flex items-center gap-2 rounded-xl border border-line bg-surface px-3 py-2'> <select value={filters.availability} onChange={(e) => setFilter('availability', e.target.value)} className='bg-transparent text-sm outline-none'> <option value=''>كل حالات التوفر</option> <option value='AVAILABLE'>متاح</option> <option value='UNAVAILABLE'>غير متاح</option> </select> </div> <div className='flex items-center gap-2 rounded-xl border border-line bg-surface px-3 py-2'> <select value={filters.status} onChange={(e) => setFilter('status', e.target.value)} className='bg-transparent text-sm outline-none'> <option value=''>كل الحالات</option> <option value='ACTIVE'>نشط</option> <option value='INACTIVE'>غير نشط</option> </select> </div> {hasActiveFilters && ( <button type='button' onClick={clearFilters} className='rounded-lg border border-line px-3 py-2 text-xs font-semibold text-ink-soft hover:bg-background'> مسح الفلاتر </button> )} </div> {loading && products.length === 0 ? ( <ProductsSkeleton /> ) : ( <> <ProductsStats loading={loading} total={pagination.total} active={activeProducts} available={availableProducts} /> <ProductsTable products={products} loading={loading} deletingId={deletingId} hasActiveFilters={hasActiveFilters} onRefresh={() => void loadProducts(page)} onCreate={openCreateForm} onEdit={openEditForm} onDelete={(product) => void deleteProduct(product)} /> </> )} {/* Pagination */} {pagination.pages > 1 && ( <div className='mt-6 flex items-center justify-center gap-3'> <button type='button' onClick={() => goToPage(page - 1)} disabled={page <= 1 || loading} className='flex items-center gap-1 rounded-xl border border-line bg-surface px-4 py-2 text-sm font-medium text-ink-soft transition hover:bg-background disabled:opacity-40' > <ChevronRightIcon width={14} height={14} /> السابق </button> <span className='text-sm text-ink-soft'> صفحة {page} من {pagination.pages} </span> <button type='button' onClick={() => goToPage(page + 1)} disabled={page >= pagination.pages || loading} className='flex items-center gap-1 rounded-xl border border-line bg-surface px-4 py-2 text-sm font-medium text-ink-soft transition hover:bg-background disabled:opacity-40' > التالي <ChevronLeftIcon width={14} height={14} /> </button> </div> )} {showForm && <ProductFormModal state={state} />} </div> ) ; }