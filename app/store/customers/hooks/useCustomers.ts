'use client' ; import { FormEvent, useEffect, useState } from 'react' ; import { useToast } from '@/app/store/components/ToastProvider' ; import type { DashboardCustomer } from '@/app/store/dashboard/hooks/useDashboardData' ; export type Customer = DashboardCustomer ; type CustomersResponse = { success: boolean; message?: string; customers?: Customer[]; customer?: Customer; } ; async function readJson(response: Response): Promise<CustomersResponse> { const data = (await response.json()) as CustomersResponse ; if (!response.ok || !data.success) { throw new Error(data.message || 'حدث خطأ في الطلب') ; } return data ; } export function useCustomers() { const { showToast } = useToast() ; const [customers, setCustomers] = useState<Customer[]>([]) ; const [loading, setLoading] = useState(true) ; const [saving, setSaving] = useState(false) ; const [searchQuery, setSearchQuery] = useState('') ; const [name, setName] = useState('') ; const [phone, setPhone] = useState('') ; const [address, setAddress] = useState('') ; const [notes, setNotes] = useState('') ; const [editingId, setEditingId] = useState<string | null>(null) ; async function loadCustomers() { try { setLoading(true) ; const url = searchQuery.trim() ? `/api/store/customers?q=${encodeURIComponent(searchQuery.trim())}&limit=100` : '/api/store/customers?limit=100' ; const response = await fetch(url, { method: 'GET', cache: 'no-store', credentials: 'include' }) ; const data = await readJson(response) ; setCustomers(data.customers || []) ; } catch (err) { console.error('Load customers failed:', err) ; showToast(err instanceof Error ? err.message : 'حدث خطأ أثناء تحميل العملاء', 'error') ; } finally { setLoading(false) ; } } useEffect(() => { void loadCustomers() ; }, [searchQuery]) ; function resetForm() { setName('') ; setPhone('') ; setAddress('') ; setNotes('') ; setEditingId(null) ; } async function handleSubmit(event: FormEvent<HTMLFormElement>) { event.preventDefault() ; if (saving) return ; const trimmedName = name.trim() ; const trimmedPhone = phone.trim() ; if (!trimmedName) { showToast('اسم العميل مطلوب', 'warning') ; return ; } if (!trimmedPhone) { showToast('رقم الهاتف مطلوب', 'warning') ; return ; } setSaving(true) ; try { if (!editingId) { const response = await fetch('/api/store/customers', { method: 'POST', headers: { 'Content-Type': 'application/json' }, credentials: 'include', body: JSON.stringify({ name: trimmedName, phone: trimmedPhone, address: address.trim() || null, notes: notes.trim() || null }), }) ; await readJson(response) ; showToast('تم إضافة العميل بنجاح', 'success') ; } else { const response = await fetch(`/api/store/customers/${editingId}`, { method: 'PATCH', headers: { 'Content-Type': 'application/json' }, credentials: 'include', body: JSON.stringify({ name: trimmedName, phone: trimmedPhone, address: address.trim() || null, notes: notes.trim() || null }), }) ; await readJson(response) ; showToast('تم تحديث بيانات العميل بنجاح', 'success') ; } resetForm() ; await loadCustomers() ; } catch (err) { console.error('Save customer failed:', err) ; showToast(err instanceof Error ? err.message : 'حدث خطأ أثناء حفظ العميل', 'error') ; } finally { setSaving(false) ; } } function startEditing(customer: Customer) { setEditingId(customer.id) ; setName(customer.name) ; setPhone(customer.phone) ; setAddress('') ; setNotes('') ; } async function handleDelete(customer: Customer) { if (saving) return ; if (!window.confirm(`هل أنت متأكد من حذف العميل '${customer.name}'؟`)) return ; try { setSaving(true) ; const response = await fetch(`/api/store/customers/${customer.id}`, { method: 'DELETE', credentials: 'include' }) ; await readJson(response) ; showToast('تم حذف العميل بنجاح', 'success') ; if (editingId === customer.id) resetForm() ; await loadCustomers() ; } catch (err) { console.error('Delete customer failed:', err) ; showToast(err instanceof Error ? err.message : 'حدث خطأ أثناء حذف العميل', 'error') ; } finally { setSaving(false) ; } } return { customers, loading, saving, searchQuery, name, phone, address, notes, editingId, setSearchQuery, setName, setPhone, setAddress, setNotes, handleSubmit, startEditing, handleDelete, resetForm } ; }