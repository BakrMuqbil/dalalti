import type { Metadata } from 'next' ; import { notFound } from 'next/navigation' ; import { prisma } from '@/lib/prisma' ; import { StoreHeader } from '../../components/StoreHeader' ; import { StoreFooter } from '../../components/StoreFooter' ; import { Button } from '@/components/ui/Button' ; import { ArrowLeftIcon } from '@/components/icons' ; import Link from 'next/link' ; import { ImageGallery } from './components/ImageGallery' ; import { VariantSelector } from './components/VariantSelector' ; import { ShareButton } from './components/ShareButton' ; export const dynamic = 'force-dynamic' ; type Props = { params: Promise<{ storeSlug: string ; productId: string }> ; } ; export async function generateMetadata({ params }: Props): Promise<Metadata> { const { storeSlug, productId } = await params ; const store = await prisma.store.findUnique({ where: { slug: storeSlug, status: 'ACTIVE' }, select: { name: true }, }) ; const product = await prisma.product.findFirst({ where: { id: productId, store: { slug: storeSlug, status: 'ACTIVE' } }, select: { name: true, description: true, images: { select: { imageUrl: true, isPrimary: true }, take: 1 }, }, }) ; if (!store || !product) { return { title: 'المنتج غير موجود | دلالتي', } ; } const primaryImage = product.images.find((img) => img.isPrimary) ?? product.images[0] ; return { title: `${product.name} | ${store.name}`, description: product.description || `تفاصيل ${product.name} من ${store.name}`, openGraph: { title: product.name, description: product.description || undefined, images: primaryImage ? [{ url: primaryImage.imageUrl }] : undefined, }, } ; } export default async function ProductDetailPage({ params }: Props) { const { storeSlug, productId } = await params ; const store = await prisma.store.findUnique({ where: { slug: storeSlug, status: 'ACTIVE' }, select: { id: true, name: true, slug: true, logoUrl: true, phone: true, description: true }, }) ; if (!store) { notFound() ; } const product = await prisma.product.findFirst({ where: { id: productId, storeId: store.id, status: 'ACTIVE' }, select: { id: true, name: true, description: true, price: true, availability: true, images: { orderBy: [ { isPrimary: 'desc' }, { sortOrder: 'asc' }, ], select: { id: true, imageUrl: true, isPrimary: true }, }, variants: { select: { id: true, color: true, size: true, price: true, availability: true }, }, category: { select: { id: true, name: true }, }, }, }) ; if (!product) { notFound() ; } const formatPrice = (value: number) => new Intl.NumberFormat('ar-YE', { maximumFractionDigits: 0 }).format(value) ; return ( <div className='flex min-h-screen flex-col bg-background'> <StoreHeader storeName={store.name} storeSlug={store.slug} logoUrl={store.logoUrl} phone={store.phone} itemCount={0} onSearch={() => {}} /> <main className='mx-auto w-full max-w-7xl flex-1 px-4 py-6 sm:px-6 lg:px-8'> {/* Breadcrumb */} <nav className='mb-6' aria-label='التنقل'> <Link href={`/${storeSlug}`} className='inline-flex items-center gap-2 text-sm text-ink-soft transition-colors hover:text-brand' > <ArrowLeftIcon className='h-4 w-4' aria-hidden /> <span>العودة للمتجر</span> </Link> </nav> <article className='grid gap-8 lg:grid-cols-2'> {/* Image Gallery */} <ImageGallery images={product.images} productName={product.name} /> {/* Product Details */} <div className='flex flex-col'> {product.category && ( <p className='mb-2 text-sm font-medium text-gold'>{product.category.name}</p> )} <div className='flex items-start justify-between gap-4'> <h1 className='font-display text-2xl font-bold tracking-tight text-ink sm:text-3xl'> {product.name} </h1> <ShareButton productName={product.name} description={product.description} /> </div> {product.description && ( <p className='mt-4 text-base leading-relaxed text-ink-soft'> {product.description} </p> )} {/* Variants */} <div className='mt-6'> <VariantSelector variants={product.variants} basePrice={product.price} /> </div> {/* Base Price (if no variants) */} {product.variants.length === 0 && ( <div className='mt-6 border-t border-line pt-6'> <div className='flex items-baseline gap-2'> <span className='font-display text-3xl font-bold text-ink'> {formatPrice(product.price)} </span> <span className='text-sm text-ink-soft'>ريال يمني</span> </div> </div> )} {/* Availability (if no variants) */} {product.variants.length === 0 && ( <div className='mt-4 flex items-center gap-2'> <span className={`inline-flex h-2.5 w-2.5 rounded-full ${ product.availability === 'AVAILABLE' ? 'bg-success' : 'bg-danger' }`} aria-hidden /> <span className={`text-sm font-medium ${ product.availability === 'AVAILABLE' ? 'text-success' : 'text-danger' }`}> {product.availability === 'AVAILABLE' ? 'متوفر' : 'غير متوفر'} </span> </div> )} {/* CTA */} <div className='mt-auto pt-8'> {product.availability === 'AVAILABLE' ? ( <Button variant='primary' size='lg' className='w-full' disabled > أضف إلى السلة </Button> ) : ( <Button variant='secondary' size='lg' className='w-full' disabled > غير متوفر حالياً </Button> )} <p className='mt-3 text-center text-xs text-ink-soft/60'> السلة قريباً — المرحلة القادمة </p> </div> </div> </article> </main> <StoreFooter storeName={store.name} /> </div> ) ; }