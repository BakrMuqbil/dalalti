'use client' ; import { ShareIcon } from '@/components/icons' ; type Props = { productName: string ; description: string | null ; } ; export function ShareButton({ productName, description }: Props) { const handleShare = () => { const url = window.location.href ; if (navigator.share) { navigator.share({ title: productName, text: description || productName, url, }) ; } else { navigator.clipboard.writeText(url) ; } } ; return ( <button type='button' onClick={handleShare} className='flex h-10 w-10 shrink-0 items-center justify-center rounded-xl border border-line bg-background transition-colors hover:bg-gold-soft/20' aria-label='مشاركة المنتج' > <ShareIcon className='h-5 w-5 text-ink-soft' /> </button> ) ; }