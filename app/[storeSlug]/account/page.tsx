import { notFound } from 'next/navigation' ; import { prisma } from '@/lib/prisma' ; import { AccountLookup } from './components/AccountLookup' ; import { StoreHeader } from '../components/StoreHeader' ; import { StoreFooter } from '../components/StoreFooter' ; import { StorefrontWrapper } from '../components/StorefrontWrapper' ; export const dynamic = 'force-dynamic' ; type Props = { params: Promise<{ storeSlug: string }> ; } ; export default async function AccountPage({ params }: Props) { const { storeSlug } = await params ; const store = await prisma.store.findUnique({ where: { slug: storeSlug, status: 'ACTIVE' }, select: { id: true, name: true, slug: true, logoUrl: true, phone: true }, }) ; if (!store) { notFound() ; } return ( <StorefrontWrapper storeSlug={store.slug}> <div className='flex min-h-screen flex-col bg-background'> <StoreHeader storeName={store.name} storeSlug={store.slug} logoUrl={store.logoUrl} phone={store.phone} onSearch={() => {}} /> <main className='mx-auto w-full max-w-7xl flex-1 px-4 py-12 sm:px-6 lg:px-8'> <AccountLookup storeSlug={store.slug} storeName={store.name} /> </main> <StoreFooter storeName={store.name} /> </div> </StorefrontWrapper> ) ; }