'use client' ; import { useEffect, useState } from 'react' ; import Link from 'next/link' ; import { useRouter } from 'next/navigation' ; import { EmptyState } from '@/components/ui/EmptyState' ; import { Button } from '@/components/ui/Button' ; import { ArrowLeftIcon, ReceiptIcon, UsersIcon } from '@/components/icons' ; import { OrderCard } from '../components/OrderCard' ; import { Spinner } from '@/components/feedback/Spinner' ; type Order = { id: string ; orderNumber: string ; status: string ; totalAmount: string ; createdAt: string ; itemCount: number ; } ; type Props = { storeSlug: string ; storeName: string ; } ; export function OrdersList({ storeSlug, storeName }: Props) { const [orders, setOrders] = useState<Order[]>([]) ; const [customer, setCustomer] = useState<{ name: string ; phone: string } | null>(null) ; const [isLoading, setIsLoading] = useState(true) ; const [error, setError] = useState<string | null>(null) ; const router = useRouter() ; useEffect(() => { const phone = sessionStorage.getItem(`dalalti-account-${storeSlug}`) ; if (!phone) { router.push(`/${storeSlug}/account`) ; return ; } async function fetchOrders() { try { const response = await fetch(`/api/public/stores/${storeSlug}/orders?phone=${encodeURIComponent(phone)}`) ; const data = await response.json() ; if (!response.ok || !data.success) { throw new Error(data.message || 'حدث خطأ') ; } setCustomer(data.customer) ; setOrders(data.orders) ; } catch (err) { setError(err instanceof Error ? err.message : 'حدث خطأ غير متوقع') ; } finally { setIsLoading(false) ; } } fetchOrders() ; }, [storeSlug, router]) ; if (isLoading) { return ( <div className='flex items-center justify-center py-24'> <Spinner size='lg' label='جاري تحميل الطلبات...' /> </div> ) ; } return ( <div className='mx-auto w-full max-w-2xl'> <nav className='mb-6' aria-label='التنقل'> <Link href={`/${storeSlug}/account`} className='inline-flex items-center gap-2 text-sm text-ink-soft transition-colors hover:text-brand' > <ArrowLeftIcon className='h-4 w-4' aria-hidden /> <span>العودة للحساب</span> </Link> </nav> <div className='mb-8'> <h1 className='font-display text-2xl font-bold text-ink'>طلباتي</h1> {customer && ( <p className='mt-1 text-sm text-ink-soft'> {customer.name} — {customer.phone} </p> )} </div> {error && ( <div className='mb-6 rounded-xl border border-danger bg-danger-bg p-4 text-sm text-danger'> {error} </div> )} {orders.length === 0 ? ( <EmptyState icon={<ReceiptIcon className='h-12 w-12 text-ink-soft' />} title='لا توجد طلبات' description='لم تقومي بأي طلبات بعد. تصفحي المتجر واختي منتجاتك المفضلة.' > <Link href={`/${storeSlug}`}> <Button variant='primary'>تصفح المنتجات</Button> </Link> </EmptyState> ) : ( <div className='space-y-4'> {orders.map((order) => ( <OrderCard key={order.id} order={order} storeSlug={storeSlug} /> ))} </div> )} </div> ) ; }