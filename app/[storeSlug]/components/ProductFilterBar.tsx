'use client' ; import { useState, useCallback } from 'react' ; import { useRouter } from 'next/navigation' ; import { SearchIcon, FilterIcon, ChevronDownIcon } from '@/components/icons' ; type Props = { storeSlug: string ; initialQuery: string ; initialSort: string ; initialAvailability: string ; } ; const sortOptions = [ { value: 'newest', label: 'الأحدث' }, { value: 'price-asc', label: 'الأقل سعراً' }, { value: 'price-desc', label: 'الأعلى سعراً' }, { value: 'name', label: 'الاسم' }, ] ; export function ProductFilterBar({ storeSlug, initialQuery, initialSort, initialAvailability }: Props) { const router = useRouter() ; const [query, setQuery] = useState(initialQuery) ; const [sort, setSort] = useState(initialSort || 'newest') ; const [availability, setAvailability] = useState(initialAvailability || 'all') ; const [isSortOpen, setIsSortOpen] = useState(false) ; const updateUrl = useCallback((params: Record<string, string>) => { const url = new URL(window.location.href) ; Object.entries(params).forEach(([key, value]) => { if (value && value !== 'all') { url.searchParams.set(key, value) ; } else { url.searchParams.delete(key) ; } }) ; router.push(url.pathname + url.search, { scroll: false }) ; }, [router]) ; const handleSearch = (value: string) => { setQuery(value) ; // Debounce URL update const timer = setTimeout(() => { updateUrl({ q: value }) ; }, 300) ; return () => clearTimeout(timer) ; } ; const handleSort = (value: string) => { setSort(value) ; setIsSortOpen(false) ; updateUrl({ sort: value }) ; } ; const handleAvailability = (value: string) => { setAvailability(value) ; updateUrl({ availability: value }) ; } ; return ( <div className='mb-6 space-y-3'> <div className='flex flex-col gap-3 sm:flex-row sm:items-center'> {/* Search */} <div className='relative flex-1'> <SearchIcon className='pointer-events-none absolute top-1/2 right-3 h-4 w-4 -translate-y-1/2 text-ink-soft' aria-hidden /> <input type='text' value={query} onChange={(e) => handleSearch(e.target.value)} placeholder='ابحثي في المنتجات...' className='w-full rounded-xl border border-line bg-background py-2.5 pr-10 pl-4 text-sm text-ink outline-none transition-colors placeholder:text-ink-soft/60 focus:border-gold focus:ring-2 focus:ring-gold/15' aria-label='البحث في المنتجات' /> </div> {/* Sort */} <div className='relative'> <button type='button' onClick={() => setIsSortOpen(!isSortOpen)} className='flex w-full items-center justify-between gap-2 rounded-xl border border-line bg-background px-4 py-2.5 text-sm text-ink transition-colors hover:border-gold sm:w-auto' aria-haspopup='listbox' aria-expanded={isSortOpen} > <span>{sortOptions.find((o) => o.value === sort)?.label || 'الأحدث'}</span> <ChevronDownIcon className={`h-4 w-4 text-ink-soft transition-transform ${isSortOpen ? 'rotate-180' : ''}`} /> </button> {isSortOpen && ( <div className='absolute right-0 top-full z-20 mt-2 w-full min-w-[10rem] rounded-xl border border-line bg-surface py-1 shadow-lg sm:w-auto' role='listbox'> {sortOptions.map((option) => ( <button key={option.value} type='button' onClick={() => handleSort(option.value)} className={`block w-full px-4 py-2 text-right text-sm transition-colors ${ sort === option.value ? 'bg-gold-soft/20 font-medium text-brand-deep' : 'text-ink-soft hover:bg-background' }`} role='option' aria-selected={sort === option.value} > {option.label} </button> ))} </div> )} </div> </div> {/* Availability Filter */} <div className='flex items-center gap-2'> <FilterIcon className='h-4 w-4 text-ink-soft' aria-hidden /> <div className='flex gap-2'> <button type='button' onClick={() => handleAvailability('all')} className={`rounded-full px-3 py-1 text-xs font-medium transition-all ${ availability === 'all' ? 'bg-brand text-white' : 'border border-line bg-surface text-ink-soft hover:border-gold' }`} aria-pressed={availability === 'all'} > الكل </button> <button type='button' onClick={() => handleAvailability('available')} className={`rounded-full px-3 py-1 text-xs font-medium transition-all ${ availability === 'available' ? 'bg-success text-white' : 'border border-line bg-surface text-ink-soft hover:border-gold' }`} aria-pressed={availability === 'available'} > متوفر فقط </button> </div> </div> </div> ) ; }