import { EmptyState } from '@/components/ui/EmptyState' ; import { PackageCheckIcon } from '@/components/icons' ; import { ProductCard } from './ProductCard' ; type Product = { id: string ; name: string ; description: string | null ; price: number ; availability: 'AVAILABLE' | 'UNAVAILABLE' ; images: { imageUrl: string ; isPrimary: boolean }[] ; variants: { id: string }[] ; category: { id: string ; name: string } | null ; } ; type Props = { products: Product[] ; storeSlug: string ; } ; export function ProductGrid({ products, storeSlug }: Props) { if (products.length === 0) { return ( <EmptyState icon={<PackageCheckIcon className='h-12 w-12 text-ink-soft' />} title='لا توجد منتجات' description='لم يتم العثور على منتجات مطابقة لبحثك. جربي كلمات مختلفة أو تصفحي جميع المنتجات.' /> ) ; } return ( <div className='grid grid-cols-2 gap-4 sm:grid-cols-3 lg:grid-cols-4'> {products.map((product) => ( <ProductCard key={product.id} product={product} storeSlug={storeSlug} /> ))} </div> ) ; }