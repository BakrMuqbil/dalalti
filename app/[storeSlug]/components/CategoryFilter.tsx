'use client' ; import Image from 'next/image' ; type Category = { id: string ; name: string ; imageUrl: string | null ; } ; type Props = { categories: Category[] ; activeCategory: string | null ; onSelect: (id: string | null) => void ; } ; export function CategoryFilter({ categories, activeCategory, onSelect }: Props) { return ( <div className='mb-8'> <div className='flex gap-2 overflow-x-auto pb-2 scrollbar-hide'> <button type='button' onClick={() => onSelect(null)} className={`flex shrink-0 items-center gap-2 rounded-full border px-4 py-2 text-sm font-medium transition-all ${ activeCategory === null ? 'border-brand bg-brand text-white' : 'border-line bg-surface text-ink-soft hover:border-gold hover:text-ink' }`} aria-pressed={activeCategory === null} > الكل </button> {categories.map((cat) => ( <button key={cat.id} type='button' onClick={() => onSelect(cat.id)} className={`flex shrink-0 items-center gap-2 rounded-full border px-4 py-2 text-sm font-medium transition-all ${ activeCategory === cat.id ? 'border-brand bg-brand text-white' : 'border-line bg-surface text-ink-soft hover:border-gold hover:text-ink' }`} aria-pressed={activeCategory === cat.id} > {cat.imageUrl && ( <div className='relative h-6 w-6 overflow-hidden rounded-full'> <Image src={cat.imageUrl} alt='' fill className='object-cover' sizes='24px' aria-hidden /> </div> )} <span>{cat.name}</span> </button> ))} </div> </div> ) ; }