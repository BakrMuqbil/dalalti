'use client' ; import Image from 'next/image' ; import Link from 'next/link' ; type Category = { id: string ; name: string ; imageUrl: string | null ; } ; type Props = { categories: Category[] ; activeCategory: string | null ; storeSlug: string ; } ; export function CategoryFilter({ categories, activeCategory, storeSlug }: Props) { return ( <div className='mb-6'> <div className='flex gap-2 overflow-x-auto pb-2 scrollbar-hide'> <Link href={`/${storeSlug}`} className={`flex shrink-0 items-center gap-2 rounded-full border px-4 py-2 text-sm font-medium transition-all ${ activeCategory === null ? 'border-brand bg-brand text-white' : 'border-line bg-surface text-ink-soft hover:border-gold hover:text-ink' }`} aria-current={activeCategory === null ? 'page' : undefined} > الكل </Link> {categories.map((cat) => ( <Link key={cat.id} href={`/${storeSlug}?category=${cat.id}`} className={`flex shrink-0 items-center gap-2 rounded-full border px-4 py-2 text-sm font-medium transition-all ${ activeCategory === cat.id ? 'border-brand bg-brand text-white' : 'border-line bg-surface text-ink-soft hover:border-gold hover:text-ink' }`} aria-current={activeCategory === cat.id ? 'page' : undefined} > {cat.imageUrl && ( <div className='relative h-6 w-6 overflow-hidden rounded-full'> <Image src={cat.imageUrl} alt='' fill className='object-cover' sizes='24px' aria-hidden /> </div> )} <span>{cat.name}</span> </Link> ))} </div> </div> ) ; }