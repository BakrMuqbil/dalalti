import type { FormEvent } from 'react' ; import { Button } from '@/components/ui/Button' ; import { Modal, ModalHeader, ModalFooter } from '@/components/ui/Modal' ; import type { Plan, StoreForm } from '../hooks/useAdminStores' ; import { PlanSelector } from './PlanSelector' ; import { StoreOwnerFields } from './StoreOwnerFields' ; import { StoreFields } from './StoreFields' ; type Props = { open: boolean ; plans: Plan[] ; loadingPlans: boolean ; creating: boolean ; form: StoreForm ; formError: string ; onClose: () => void ; onSubmit: (event: FormEvent<HTMLFormElement>) => void ; onChange: (field: keyof StoreForm, value: string) => void ; onClearError: () => void ; } ; export function AddStoreModal({ open, plans, loadingPlans, creating, form, formError, onClose, onSubmit, onChange }: Props) { return ( <Modal open={open} onClose={onClose} size='md'> <ModalHeader label='NEW STORE' title='إضافة متجر جديد' description='إنشاء المالك والمتجر والاشتراك تلقائيًا' onClose={onClose} closeDisabled={creating} /> <form onSubmit={onSubmit} className='space-y-6 p-6'> {formError && <div className='rounded-xl border border-danger/25 bg-danger-bg p-4 text-sm text-danger'>{formError}</div>} <StoreOwnerFields form={form} onChange={onChange} /> <StoreFields form={form} onChange={onChange} /> <PlanSelector plans={plans} loading={loadingPlans} selectedPlanId={form.planId} onSelect={(value) => onChange('planId', value)} /> <ModalFooter> <Button type='button' variant='secondary' size='lg' onClick={onClose} disabled={creating} className='bg-white text-ink-soft hover:bg-background'> إلغاء </Button> <Button type='submit' variant='primary' size='lg' loading={creating} disabled={creating || loadingPlans || plans.length === 0} className='px-6 hover:bg-brand-dark'> {creating ? 'جاري إنشاء المتجر...' : 'إنشاء المتجر'} </Button> </ModalFooter> </form> </Modal> ) ; }