import { StatsSkeleton } from '@/components/ui/skeletons/StatsSkeleton' ; import { StatCard } from '@/components/ui/StatCard' ; import type { Store } from '../hooks/useAdminStores' ; type Props = { stores: Store[] ; loading: boolean } ; export function StoresStats({ stores, loading }: Props) { if (loading) { return ( <div className='mb-6'> <StatsSkeleton count={3} /> </div> ) } const activeStores = stores.filter((store) => store.status === 'ACTIVE').length ; const activeSubscriptions = stores.filter((store) => store.subscription?.status === 'ACTIVE').length ; return ( <div className='mb-6 grid gap-4 sm:grid-cols-3'> <StatCard label='إجمالي المتاجر' value={stores.length} tone='ink' /> <StatCard label='المتاجر النشطة' value={activeStores} tone='success' /> <StatCard label='الاشتراكات النشطة' value={activeSubscriptions} tone='brand' /> </div> ) ; }