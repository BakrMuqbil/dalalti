export async function readJson<T = any>(response: Response, defaultMessage = 'حدث خطأ في الطلب'): Promise<T> { const data = (await response.json()) as T & { success?: boolean; message?: string } ; if (!response.ok || !data.success) { throw new Error(data.message || defaultMessage) ; } return data as T ; } export async function fetchWithAuth(url: string, options?: RequestInit): Promise<Response> { return fetch(url, { cache: 'no-store', credentials: 'include', ...options, }) ; }