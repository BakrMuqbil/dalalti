import { ReactNode } from 'react' ; import Link from 'next/link' ; type Props = { label: string ; value: string | number ; hint?: string ; icon?: ReactNode ; tone?: 'brand' | 'success' | 'gold' | 'danger' | 'ink' | 'neutral' ; href?: string ; highlight?: boolean ; className?: string ; } ; const toneClasses: Record<NonNullable<Props['tone']>, string> = { brand: 'bg-brand/10 text-brand', gold: 'bg-gold/10 text-gold', success: 'bg-success/10 text-success', danger: 'bg-danger/10 text-danger', ink: 'bg-ink/8 text-ink-soft', neutral: 'bg-background text-ink-soft' } ; const valueToneClasses: Record<NonNullable<Props['tone']>, string> = { brand: 'text-brand', gold: 'text-gold', success: 'text-success', danger: 'text-danger', ink: 'text-ink', neutral: 'text-ink' } ; export function StatCard({ label, value, hint, icon, tone = 'ink', href, highlight = false, className = '' }: Props) { const content = ( <article className={`h-full rounded-2xl border bg-surface p-5 transition duration-200 ${ highlight ? 'border-danger/40 shadow-[0_2px_12px_rgba(166,75,75,0.10)]' : 'border-line shadow-sm' } ${href ? 'hover:-translate-y-0.5 hover:border-gold/40 hover:shadow-md' : ''} ${className}`} > <div className='flex items-start justify-between gap-4'> <div className='min-w-0'> <p className='text-sm font-medium text-ink-soft'>{label}</p> <p className={`mt-3 font-display text-3xl font-bold tracking-tight ${valueToneClasses[tone]}`}> {value} </p> {hint && <p className='mt-1.5 text-xs text-ink-soft/80'>{hint}</p>} </div> {icon && ( <div className={`flex h-10 w-10 shrink-0 items-center justify-center rounded-xl ${toneClasses[tone]}`} aria-hidden> {icon} </div> )} </div> </article> ) ; return href ? ( <Link href={href} className='block h-full'> {content} </Link> ) : content ; }