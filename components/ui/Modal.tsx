import { ReactNode } from 'react' ; import { Button } from './Button' ; type Props = { open: boolean ; onClose: () => void ; children: ReactNode ; size?: 'sm' | 'md' | 'lg' | 'xl' ; className?: string ; } ; const sizeClasses = { sm: 'max-w-lg', md: 'max-w-2xl', lg: 'max-w-4xl', xl: 'max-w-5xl' } ; export function Modal({ open, onClose, children, size = 'md', className = '' }: Props) { if (!open) return null ; return ( <div className='fixed inset-0 z-50 flex items-center justify-center bg-ink/55 p-4 backdrop-blur-sm' onMouseDown={(e) => { if (e.target === e.currentTarget) onClose() }}> <div className={`max-h-[92vh] w-full overflow-y-auto rounded-3xl border border-line bg-white shadow-2xl ${sizeClasses[size]} ${className}`}> {children} </div> </div> ) ; } type ModalHeaderProps = { label?: string ; title: string ; description?: string ; onClose?: () => void ; closeDisabled?: boolean ; } ; export function ModalHeader({ label, title, description, onClose, closeDisabled }: ModalHeaderProps) { return ( <div className='flex items-center justify-between border-b border-line px-6 py-5'> <div> {label && <span className='text-xs font-medium text-gold'>{label}</span>} <h2 className='mt-1 text-xl font-bold text-ink'>{title}</h2> {description && <p className='mt-1 text-sm text-ink-soft'>{description}</p>} </div> {onClose && ( <Button type='button' variant='ghost' size='sm' onClick={onClose} disabled={closeDisabled} aria-label='إغلاق' className='text-ink-soft hover:bg-background'> ✕ </Button> )} </div> ) ; } type ModalFooterProps = { children: ReactNode ; className?: string ; } ; export function ModalFooter({ children, className = '' }: ModalFooterProps) { return ( <div className={`flex justify-end gap-3 border-t border-line px-6 py-5 ${className}`}> {children} </div> ) ; }